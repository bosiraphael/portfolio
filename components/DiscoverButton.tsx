const DiscoverButton = () => {
  return (
    <button
      // onClick={() => {
      //   window.scrollTo({
      //     top: window.innerHeight,
      //     behavior: "smooth",
      //   });
      // }}
      className="discover-button"
    >
      Discover
    </button>
  );
};

export default DiscoverButton;
